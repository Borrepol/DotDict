# -----------------------------------------------------
# General

# Python cache
__pycache__/
# Folder containing stub files created by mypy, in all folders
out/
# All hidden files
**/.*
# Exception from the above rule for .gitignore
!.gitignore
# All files starting with `__`, in all folders
# **/__* Commented out for now because the exception with __init.py__ doesn't work


# -----------------------------------------------------
# Items/folders specific to this project

# Exception from the above rule for __init.py__
!__init.py__

# -----------------------------------------------------
# Notes

# `[folder_name]/` matches that folder in all subfolders, not just the top folder
# A leading `**/[pattern]` matches the pattern in all folders
# For more info, see: https://git-scm.com/docs/gitignore